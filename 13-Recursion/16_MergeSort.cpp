#include<iostream>
#include<vector>
using namespace std;

void merge(vector<int> &a, vector<int> &b, vector<int> &c){
    
}

int main(){
 in n=
}


// step 1: is to create two chotte chote array of size half of each but in odd size mein it will be n and n+1 size
// step 2: call merge sort on both of them
// step 3: merge both of them
